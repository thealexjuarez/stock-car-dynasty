import { View } from 'react-native'; import { AppCard } from '@/components/shared/app-card'; import { AppRow } from '@/components/shared/app-row'; import { AppText } from '@/components/shared/app-text'; import { Screen } from '@/components/shared/screen'; import { StatusBadge } from '@/components/shared/status-badge'; import { starterGameState } from '@/data/starter-game-state'; import { theme } from '@/theme';
export function VehicleDetailScreen({ number }:{ number:string }) { const v=starterGameState.vehicles.find(x=>x.number===number); const d=starterGameState.drivers.find(x=>x.id===v?.assignedDriverId); if(!v)return <Screen><AppText>Vehicle not found.</AppText></Screen>; return <Screen><View style={{ gap:theme.spacing.sm }}><AppText variant="eyebrow" tone="accent">Apex Motorsports · Ranger Performance</AppText><AppText variant="hero">Car #{v.number}</AppText><AppText tone="muted">Assigned to {d?.name}. Both active cars receive equal upgrade treatment.</AppText></View><AppCard><View style={{ flexDirection:'row',justifyContent:'space-between',gap:theme.spacing.md }}><AppText variant="title">Vehicle Status</AppText><StatusBadge label={`${v.condition}% condition`} tone={v.condition>=90?'green':'yellow'} /></View><AppRow label="Performance" detail={`${v.performance}`} /><AppRow label="Chassis Wear" detail={v.chassisWear} /><AppRow label="Engine Wear" detail={v.engineWear} /></AppCard><AppCard><AppText variant="title">Garage Note</AppText><AppText tone="muted">{v.note}</AppText></AppCard><AppCard><AppText variant="title">Next Action</AppText><AppText tone="muted">Review condition after each race. Repairs are car-specific; performance upgrades apply evenly to both entries.</AppText></AppCard></Screen> }
